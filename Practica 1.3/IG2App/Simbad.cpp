#include "Simbad.h"

Simbad::Simbad(Ogre::SceneNode* node):EntidadIG(node)
{
	//mNode = mSM->getRootSceneNode()->createChildSceneNode("simbad");
	/*Ogre::Entity* e = mSM->createEntity("Sinbad.mesh");
	mNode->attachObject(e);
	mNode->scale(15, 15, 15);
	mNode->translate(-450, -125, 300);*/
}
